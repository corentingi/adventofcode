{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run script",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "cwd": "${workspaceFolder}",
            "justMyCode": false
        },
    ]
}
